<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huangxiaoyan.hdao.IDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->
    <select id="queryById" resultType="User" parameterType="long">
        <!-- 具体的sql -->
        SELECT
        id,
        username,
        mail,
        sex,
        mail,
        province,
        hobby
        FROM
        user_table
        WHERE
        id = #{userId}
    </select>

    <select id="queryAll" resultType="User">
   SELECT
    id,
    username,
    mail,
    sex,
    mail,
    province,
    hobby
    FROM
    user_table
    ORDER BY
    id
  </select>

    <select id="queryByUsername" resultType="User" parameterType="String">
   SELECT
    id,
    username,
    mail,
    sex,
    mail,
    province,
    hobby
    FROM
    user_table t
    WHERE t.username like #{username}
    ORDER BY
    id
  </select>

    <update id="updateUser" parameterType="User">
    UPDATE user_table
    SET
    username = #{username},
    mail = #{mail},
    sex = #{sex},
    province = #{province},
    hobby = #{hobby}
    WHERE
    id = #{id}
  </update>

    <insert id="addUser">
    INSERT INTO user_table(`username`, `mail`, `province`,`hobby`,`sex`)
    VALUES(#{username}, #{mail}, #{province}, #{hobby}, #{sex})
  </insert>

    <delete id="deleteUserById">
    DELETE FROM user_table WHERE id = #{userId}
  </delete>
</mapper>